#include "Gleam.h"
#include "TextureSource.h"

using namespace GEditor;

TextureSource TextureSource::Import(const Gleam::Filesystem::Path& path, const ImportSettings& settings)
{
	return TextureSource();
}
